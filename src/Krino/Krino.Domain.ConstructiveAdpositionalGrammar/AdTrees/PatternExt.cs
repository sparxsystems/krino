using Krino.Domain.ConstructiveAdpositionalGrammar.Constructions;

namespace Krino.Domain.ConstructiveAdpositionalGrammar.AdTrees
{
    public static class PatternExt
    {
        //public static bool CanAttachToRight(this IPattern pattern, IAdTree adTree)
        //{
        //    // If the rule for the right child matches with the adTree.
        //    if (pattern.RightRule.IsMatch(adTree.Morpheme.Morph, adTree.Morpheme.Attributes, adTree.Pattern.PatternAttributes))
        //    {
        //        // If the pattern rule for the adposition matches the adtree.
        //        if (adTree.Pattern.AdPositionRule.IsMatch(pattern.MorphemeRule.RequiredMorph, pattern.MorphemeRule.RequiredAttributes, pattern.PatternAttributes))
        //        {
        //            return true;
        //        }
        //    }

        //    return false;
        //}

        //public static bool CanAttachToRight(this IPattern pattern, IPattern patternToAttach)
        //{
        //    // If the rule for the right child matches with the adTree.
        //    if (pattern.RightRule.IsMatch(patternToAttach.MorphemeRule.RequiredMorph, patternToAttach.MorphemeRule.RequiredAttributes, patternToAttach.PatternAttributes))
        //    {
        //        // If the pattern rule for the adposition matches the adtree.
        //        if (patternToAttach.AdPositionRule.IsMatch(pattern.MorphemeRule.RequiredMorph, pattern.MorphemeRule.RequiredAttributes, pattern.PatternAttributes))
        //        {
        //            return true;
        //        }
        //    }

        //    return false;
        //}

        //public static bool CanAttachToLeft(this IPattern pattern, IAdTree adTree)
        //{
        //    // If the rule for the right child matches with the adTree.
        //    if (pattern.LeftRule.IsMatch(adTree.Morpheme.Morph, adTree.Morpheme.Attributes, adTree.Pattern.PatternAttributes))
        //    {
        //        // If the pattern rule for the adposition matches the adtree.
        //        if (adTree.Pattern.AdPositionRule.IsMatch(pattern.MorphemeRule.RequiredMorph, pattern.MorphemeRule.RequiredAttributes, pattern.PatternAttributes))
        //        {
        //            return true;
        //        }
        //    }

        //    return false;
        //}

        //public static bool CanAttachToLeft(this IPattern pattern, IPattern patternToAttach)
        //{
        //    // If the rule for the right child matches with the adTree.
        //    if (pattern.LeftRule.IsMatch(patternToAttach.MorphemeRule.RequiredMorph, patternToAttach.MorphemeRule.RequiredAttributes, patternToAttach.PatternAttributes))
        //    {
        //        // If the pattern rule for the adposition matches the adtree.
        //        if (patternToAttach.AdPositionRule.IsMatch(pattern.MorphemeRule.RequiredMorph, pattern.MorphemeRule.RequiredAttributes, pattern.PatternAttributes))
        //        {
        //            return true;
        //        }
        //    }

        //    return false;
        //}
    }
}
